{ Nada de adentro debe contarse como token }
{ x := 5; ++ <= ** NOT OR }
(* Igual aquí: a := 10; <> >= *)
BEGIN
  a:=1; b:=2;
  r := a++ + b--;      { probar ++ y -- }
  p := a**b;           { potencia ** debe comerse como token largo }
  cmp1 := a <= b; cmp2 := a >= b; cmp3 := a <> b; cmp4 := a = b;
  br := (a + b) * (a - b) / a;
  arr[1] := arr[2];
END
